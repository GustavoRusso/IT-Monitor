= form_for @monitor_summary do |f|
  - if @monitor_summary.errors.any?
    #error_explanation
      %h2= "#{pluralize(@monitor_summary.errors.count, "error")} prohibited this monitor_summary from being saved:"
      %ul
        - @monitor_summary.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :active_incidents
    = f.number_field :active_incidents
  .actions
    = f.submit 'Save'
